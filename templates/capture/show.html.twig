{% extends 'base.html.twig' %}

{% block title %}Capture{% endblock %}

{% block body %}
    <h2>Capture</h2>

    {% include 'components/table/_table_show.html.twig' with {
        object: capture,
        fields: [
            { label: 'Nom', property: 'name' },
            { label: 'Description', property: 'description' },
        ]
    } %}
    <h4>Eléments de capture</h4>
    {% include 'components/table/_table.html.twig' with {
        items: capture.captureElements,
        columns: ['name', 'description'],
        headers: ['Nom', 'Description'],
        filter: false,
        sort: false,
    } %}

    {% include 'components/button/_btn_bar.html.twig' with {
        buttons: {
            back: {
                type: 'nac-back',
                icon: 'bi-arrow-left',
                label: 'Retour',
                class: 'btn btn-outline-primary btn-sm'
            }
        }
    } %}
{% endblock %}
