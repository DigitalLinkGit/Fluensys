<form method="post" action="{{ path('app_capture_element_delete', {'id': capture_element.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ capture_element.id) }}">
    <button class="btn">Delete</button>
</form>
