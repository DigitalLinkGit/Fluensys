{% extends 'base.html.twig' %}

{% block title %}CaptureElement{% endblock %}

{% block body %}
    <h1>CaptureElement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ capture_element.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ capture_element.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ capture_element.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_capture_element_index') }}">back to list</a>

    <a href="{{ path('app_capture_element_edit', {'id': capture_element.id}) }}">edit</a>

    {{ include('capture_element/_delete_form.html.twig') }}
{% endblock %}
