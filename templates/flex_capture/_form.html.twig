{{ form_start(form) }}
    {{ include('capture_element/_form_header.html.twig') }}
<button class="btn">{{ button_label|default('Save') }}</button>
<br>
<div class="container">
    <div class="row">
        {% for field in form.children.fields %}
            <div class="card col-4">
                {{ form(field, {'attr': {'class': 'mt-1'}}) }}
            </div>
        {% endfor %}
    </div>
</div>
{{ form_end(form) }}
